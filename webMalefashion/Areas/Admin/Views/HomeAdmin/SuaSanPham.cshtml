@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model webMalefashion.Models.Product

@{
    ViewData["Title"] = "SuaSanPham";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<!-- Latest compiled and minified CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">

<!-- Latest compiled JavaScript -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
<div class="col-11 mx-auto pt-0">
    <div class="card">
        <div class="card-header bg-primary text-uppercase text-white">
            <h3>Sua San Pham </h3>
        </div>
        <div class="container-fluid">
            <div class="row">

            <div class="col-md-4">
                <form method="post" asp-action="SuaSanPham" enctype="multipart/form-data">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="Id" class="control-label"></label>
                        <input asp-for="Id" class="form-control" />
                        <span asp-validation-for="Id" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Description" class="control-label"></label>
                        <input asp-for="Description" class="form-control" />
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="CategoryId" class="control-label"></label>
                        <select asp-for="CategoryId" class="form-control" asp-items="ViewBag.CategoryId"></select>
                    </div>
                    <div class="form-group">
                        <label asp-for="ManufacturerId" class="control-label"></label>
                        <select asp-for="ManufacturerId" class="form-control" asp-items="ViewBag.ManufacturerId"></select>
                    </div>

                    <div class="container col-12">
                        <table class="table table-striped table-sm" id='ExpTable'>
                            <thead>
                                <tr>
                                    <th>
                                        ID
                                    </th>
                                    <th>
                                        ColoeHex
                                    </th>
                                    <th>
                                        ImageUrl
                                    </th>
                                    <th>
                                        Image
                                    </th>
                                    <th>
                                        Price
                                    </th>
                                    <th>
                                        SizeID
                                    </th>
                                    <th>
                                        <button id='btnadd' type="button" class="btn btn-sm btn-secondary visible"
                                                onclick="AddItem(this)">
                                            Add
                                        </button>
                                    </th>                                   
                                </tr>
                            </thead>
                            <tbody>
                                @for (int i = 0; i < Model.Options.Count; i++)
                                {
                                    <tr>
                                        <td>
                                            
                                            <input asp-for="@Model.Options[i].Id" id="idOption" class="form-control" style="width:70px" />
                                            <span asp-validation-for="@Model.Options[i].Id" class="text-danger"></span>
                                        </td>
                                        <td>

                                            <input asp-for="@Model.Options[i].ColorHex" id="colorOption" class="form-control" style="width:100px" />
                                            <span asp-validation-for="@Model.Options[i].ColorHex" class="text-danger"></span>
                                        </td>
                                        <td>
                                          
                                          
                                            <input asp-for="@Model.Options[i].ImageUrl" class="form-control" style="width:230px" />
                                            <div class="custom-file">
                                                <input asp-for="@Model.Options[i].ProductPhoto" class="custom-file-input" id="customFile">
                                            </div>
                                            <span asp-validation-for="@Model.Options[i].ProductPhoto" class="text-danger"></span>                                          
                                        </td>
                                        <td>
                                            <img src="../malefashion/img/product/@Model.Options[i].ImageUrl" width="50" height="50"
                                                 style="border:1px" />
                                        </td>
                                        <td>

                                            <input asp-for="@Model.Options[i].Price" id="priceOption" class="form-control" style="width:100px" />
                                            <span asp-validation-for="@Model.Options[i].Price" class="text-danger"></span>
                                        </td>
                                        <td>

                                            <input asp-for="@Model.Options[i].SizeId" id="sizeOption" class="form-control" style="width:100px" />
                                            <span asp-validation-for="@Model.Options[i].SizeId" class="text-danger"></span>
                                        </td>
                                        <td></td>
                                        <td>
                                            
                                            <button id='btnremove-@i' type="button" class="btn btn-sm btn-secondary visible"
                                                    onclick="DeleteItem(this)">
                                                Delete
                                            </button>
                                        </td>

                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                    <input type="hidden" id='hdnLastIndex' value="0" />
                    <div class="form-group">
                        <input type="submit" value="Save" class="btn btn-primary" />
                    </div>

                </form>
            </div>

        </div>
        </div>
    </div>
</div>

<div class="container-fluid">
    <a asp-action="DanhMucSanPham">Back to List</a>
</div>

<script type="text/javascript">
    $(".custom-file-input").on("change", function () {
        var fileName = $(this).val().split("\\").pop();
        $(this).siblings(".custom-file-label").addClass("selected").html(fileName);
    });
    function DeleteItem(btn) {
        var table = document.getElementById('ExpTable');
        var rows = table.getElementsByTagName('tr');
        if(rows.length==2){
            alter("this row cannot be deleted");
            return
        }        
        $(btn).closest('tr').remove();
    }
    function AddItem(btn) {

        var table = document.getElementById('ExpTable');
        var rows = table.getElementsByTagName('tr');

        var rowOuterHtml = rows[rows.length - 1].outerHTML;

        var lastrowIdx = rows.length - 2;

        var nextrowIdx = eval(lastrowIdx) + 1;

        document.getElementById('hdnLastIndex').value = nextrowIdx;
        document.getElementById('idOption').value  = null
        document.getElementById('colorOption').value = null 
        document.getElementById('priceOption').value = null
        document.getElementById('sizeOption').value = null

        rowOuterHtml = rowOuterHtml.replaceAll('_' + lastrowIdx + '_', '_' + nextrowIdx + '_');
        rowOuterHtml = rowOuterHtml.replaceAll('[' + lastrowIdx + ']', '[' + nextrowIdx + ']');
        rowOuterHtml = rowOuterHtml.replaceAll('-' + lastrowIdx, '-' + nextrowIdx);


        var newRow = table.insertRow();
        newRow.innerHTML = rowOuterHtml;


        var x = document.getElementsByTagName("input");
        
    }
</script>